syntax on
set ruler
set autoindent
set number
set ignorecase
color elflord
To get these configurations I had to create my file through vim .vimrc.Once I put the functions in my file I saved and quit my .vimrc file using esc shift colon then 'wq'. To display all of my configurations I used the command 'cat .vimrc' to put them into a list. 
